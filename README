The SK80 Library desperately needs a better name, but it's designed to add the ES5 methods to all browsers. It's also written to be easy to modify.

Currently it only supports Arrays, but it works in Firefox, Opera, Safari, Chrome and IE as far back as 6.